{
  "functions": {
    "api/index.ts": {
      "runtime": "@vercel/node@5.0.0"
    }
  },
  "installCommand": "pnpm install --filter ./apps/api... --ignore-scripts",
  "buildCommand": "echo 'No linting, no typechecking, just deploy API 🚀'",
  "outputDirectory": ".",
  "ignoreCommand": "echo 'Ignoring lint/typecheck'",
  "env": {
    "VERCEL_LINT": "0"
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" }
  ]
}
