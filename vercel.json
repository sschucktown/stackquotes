{
  "functions": {
    "api/index.ts": {
      "runtime": "@vercel/node@5.0.0"
    }
  },
  "installCommand": "pnpm install --filter ./apps/api... --ignore-scripts",
  "buildCommand": "echo 'Skipping lint and TypeScript build (already compiled)'",
  "outputDirectory": ".",
  "ignoreCommand": "echo 'Skipping ESLint and typecheck for Vercel build'",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" }
  ]
}
